# cli-anything-obs-studio

Command-line harness for OBS Studio scene collections.

The harness operates on a JSON project format that mirrors common OBS concepts:
scenes, sources, filters, transitions, streaming settings, and recording
settings. It does not require OBS Studio to be installed for tests or offline
project generation.
